import AdminUISettings from "@/components/AdminUISettings";

export const metadata = {
  title: "UI Settings & Store Control | Bannira Admin",
};

export default function SettingsPage() {
  return <AdminUISettings />;
}